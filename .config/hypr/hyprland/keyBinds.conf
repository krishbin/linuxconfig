$scriptsDir = ~/.config/hypr/scripts/
$mainMod = SUPER # Sets "Windows" key as main modifier

bind = $mainMod, Q, killactive,
bind = $mainMod+Ctrl+Alt, backspace, exit,
bind = $mainMod, V, togglefloating,
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, O, togglesplit, # dwindle
bind = $mainMod, F, fullscreen, # dwindle

bind = Alt, C, centerwindow, # dwindle
bind = Alt, R, resizeactive,exact 50% 50% # dwindle

bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# System function control
bind = ,XF86MonBrightnessUp, exec, $scriptsDir/brightness-ctrl.sh +
bind = ,XF86MonBrightnessDown, exec, $scriptsDir/brightness-ctrl.sh -
bind = ,XF86AudioRaiseVolume, exec, $scriptsDir/volume-ctrl.sh +
bind = ,XF86AudioLowerVolume, exec, $scriptsDir/volume-ctrl.sh -
bind = ,XF86AudioMute, exec, $scriptsDir/volume-ctrl.sh mute
bind = ,XF86AudioPlay, exec, playerctl play-pause
bind = ,XF86AudioNext, exec, playerctl next || playerctl position `bc <<< "100 * $(playerctl metadata mpris:length) / 1000000 / 100"`
bind = ,XF86AudioPrev, exec, playerctl previous

bind = $mainMod+Shift, backspace, exec, ~/.local/bin/change_input_mode.sh

bind = ,Print, exec, $scriptsDir/screenshot.sh -f
bind = Shift,Print, exec, $scriptsDir/screenshot.sh -w
bind = Ctrl,Print, exec, $scriptsDir/screenshot.sh -s

bind = WIN, F1, exec, ~/.config/hypr/scripts/gamemode.sh


# will switch to a submap called resize
bind=$mainMod,R,submap,resize

# will start a submap called "resize"
submap=resize

# sets repeatable binds for resizing the active window
binde=,right,resizeactive,20 0
binde=,left,resizeactive,-20 0
binde=,up,resizeactive,0 -20
binde=,down,resizeactive,0 20

binde=,l,resizeactive,20 0
binde=,h,resizeactive,-20 0
binde=,k,resizeactive,0 -20
binde=,j,resizeactive,0 20

# use reset to go back to the global submap
bind=,escape,submap,reset

# will reset the submap, which will return to the global submap
submap=reset
# keybinds further down will be global again...

bindm = ALT, mouse:272, resizewindow

bind = $mainMod+Shift, h,swapwindow, l
bind = $mainMod+Shift, j,swapwindow, d
bind = $mainMod+Shift, k,swapwindow, u
bind = $mainMod+Shift, l,swapwindow, r

bind = $mainMod+Ctrl, h,movewindow, l
bind = $mainMod+Ctrl, j,movewindow, d
bind = $mainMod+Ctrl, k,movewindow, u
bind = $mainMod+Ctrl, l,movewindow, r

bind = $mainMod, i, focusmonitor, -1
bind = $mainMod, o, focusmonitor, +1

bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

bind = $mainMod+Shift, u, focusurgentorlast,


# will switch to a submap called resize
bind=$mainMod+Shift,G,submap,group

# will start a submap called "resize"
submap=group

# sets repeatable binds for resizing the active window
binde=,t,togglegroup,
binde=,o,lockactivegroup,lock
binde=,u,lockactivegroup,unlock
binde = $mainMod, H, movefocus, l
binde = $mainMod, L, movefocus, r
binde = $mainMod, K, movefocus, u
binde = $mainMod, J, movefocus, d
binde=,h,moveintogroup,l
binde=,l,moveintogroup,r
binde=,k,moveintogroup,d
binde=,j,moveintogroup,u
binde=,d,moveoutofgroup,

# use reset to go back to the global submap
bind=,escape,submap,reset

# will reset the submap, which will return to the global submap
submap=reset
# keybinds further down will be global again...
#
bind=$mainMod+Shift,B,submap,bindtowindow

# will start a submap called "resize"
submap=bindtowindow

bind = ,XF86MonBrightnessUp, exec, $scriptsDir/brightness-ctrl.sh +
bind = ,XF86MonBrightnessDown, exec, $scriptsDir/brightness-ctrl.sh -
# sets repeatable binds for resizing the active window
# use reset to go back to the global submap
bind=,Print,submap,reset

# will reset the submap, which will return to the global submap
submap=reset
# keybinds further down will be global again...

bind = $mainMod, equal, workspace, m+1
bind = $mainMod, minus, workspace, m-1

bind = SUPER,Tab,cyclenext,          # change focus to another window
bind = SUPER,Tab,bringactivetotop,   # bring it to the top
bind = CTRL+$mainMod, comma, movecurrentworkspacetomonitor, l
bind = CTRL+$mainMod, period, movecurrentworkspacetomonitor, r

# application
bind = $mainMod, Return, exec, $terminal
bind = $mainMod, B, exec, $browser
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, Space, exec, $menu
bindl = , switch:on:Lid Switch, exec, $scriptsDir/handle_sleep_behavior.sh
bindl = , switch:off:Lid Switch, exec, $scriptsDir/reload-monitor.sh

bind = $mainMod+Shift, Space, exec, 1password --quick-access
bind = CTRL+$mainMod+Shift, L, exec, 1password --lock
bind = $mainMod+Shift, P, exec, 1password --toggle
bind = CTRL+$mainMod+SHIFT, G, exec, $scriptsDir/toggle_gamemode.sh game
